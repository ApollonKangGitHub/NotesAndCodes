
void print1(int * a, int size)
{
	printf("排序前为：\n");
	for(int i = 0; i<size; i++)
	{
		printf("%d  ",a[i]);
	}
	putchar('\n');
	return;
}


void print2(int * a, int size)
{
	printf("排序后为：\n");
	for(int i = 0; i<size; i++)
	{
		printf("%d  ",a[i]);
	}
	putchar('\n');
	return;
}