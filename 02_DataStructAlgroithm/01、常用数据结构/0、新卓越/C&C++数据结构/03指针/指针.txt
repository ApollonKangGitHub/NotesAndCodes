1、地址：Address

指针不是地址，指针是指向关系，是计算地址的运算方式，地址的作用是系统用来查找数据位置的依据，用来标识一块存储单元的空间。
2、地址的表示方法：

地址是一个整数，用十六进制表示。编制方式：按字节编址（每个字节都有一个唯一的地址）和按字编址

3、& 是单目运算符，结合性自右向左
4、->与.与[]为运算级别最高的运算符（且都是双目）
5、指针变量必须初始化，否则空指针指向不明，操作会带来安全隐患。int * p = 0;或者int * p = NULL;