(√)①为什么open的必须项只能选一个，而可选项可以选多个。怎样的实现机制？答：位或的机制。
(√)②创建一个10M的空文件(如迅雷下载时先创建一个空文件才向空文件中下载)
(√)③输出重定向，当C标准printf打印时直接打印到test.txt文件中(注意：不能在运行时输出重定向，即不能"./a.out > test.txt")
(√)④mycp拷贝递归实现(拷贝目录及文件)					
⑤获取屏幕的分辨率(不一定是fb0，也可以获取图形界面的分辨率)

①实现rm -rf 删除目录(unlink、rmdir)
(√)②实现ls -l(stat,opendir,readdir...)					
③从一个文件中读取1000个随机数，进行排序并写入另一个文件中(dup、dup2)

①实现多进程拷贝
②实现多进程打印

①用pipe实现本地聊天室。
(√)②思考为什么fork与pipe时，必须关掉不用的两个描述符。答：防止多余的描述符造成不适宜的双工通信方式，致使数据紊乱。

①实现mysleep函数
②实现myshell

(√)①每隔10s在/tmp/daemon.log中写入当前时间
②编写监控/home/usr/目录下文件创建与更改的守护进程，如果发生修改则将修改信息放在日志文件/home/usr/filechange.log

①实现多线程拷贝(对于过大的文件mmap分片映射)：./mycp fileold filenew N(N位指定线程数,也可以不要N参数，程序自动识别文件大小创建合理的线程数目),(迅雷下载其实就是一个网络版的多线程拷贝)，实现拷贝完成进度条/百分比。
②实现多线程检索(查找)

